
services:
  ctf-env:
    build:
      context: ./ctf-env
      dockerfile: Dockerfile
    container_name: ctf-env
    image: ctf-env
    volumes:
      - .:/home/ctf
    ports:
      - "6667:6667"
    stdin_open: true
    tty: true
